export const __esModule: boolean;
declare namespace _default {
    const clearMocks: boolean;
    const collectCoverage: boolean;
    const coverageDirectory: string;
    const coverageProvider: string;
    const testTimeout: number;
    const collectCoverageFrom: string[];
    const modulePathIgnorePatterns: string[];
    const preset: string;
    namespace coverageThreshold {
        namespace global {
            const branches: number;
            const functions: number;
            const lines: number;
            const statements: number;
        }
    }
}
export default _default;
